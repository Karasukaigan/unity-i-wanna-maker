using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Platformer.Mechanics
{
    /// <summary>
    /// 用于测试的类，通常被添加在GameController物体上，且取消对勾，不启用。
    /// </summary> 
    public class GameTest : MonoBehaviour
    {
        void Start()
        {
            //Initialization(); //初始化
        }
        
        void Initialization()
        {
            //PlayerPrefs.DeleteAll();
        }
    }

}